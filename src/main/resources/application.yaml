server:
  port: 8080

app:
  upload-dir: uploads

spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  application:
    name: somshare
  profiles:
    active: dev
  logging:
    level:
      org.springframework.security: DEBUG
